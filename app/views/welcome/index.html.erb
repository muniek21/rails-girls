<!doctype html>
<html>
  <head>
    <title>My Little Webapp: Coding Is Magic</title>
    <meta charset="UTF-8" />
    <link rel="stylesheet" href="https://rawgithub.com/krzysztofbialek/Rails-Girls-Warsaw-App/master/style.css" />
  </head>
  <body>
    <% current_user %>
    <% if signed_in? %>
    <p>You are logged in as <%= link_to(current_user.login, user_path(current_user)) %></p>
    <p><%= link_to("Sign out", signout_path, method: :delete) %></p>
    <p></p>
    <% else %>
    <p><%= link_to("Sign in", new_session_path) %></p>
    <p><%= link_to("Create new user", new_user_path) %></p>
    <% end %>
    
    <div>
  <h1>My favourite websites</h1>
    <ul>
      <% for url, name in @websites %>
  <li><a href="<%= url %>"><%= name %></a></li>
<% end %>
    </ul>
  </div>
  <div>
    <% for entry in @entries%>
    <h2><%= link_to(entry.title, entry_path(entry)) %></h2>
    <p><%= entry.contents %></p>
    <% end %>
  </div>
    
  <img src="http://railsgirls.com/images/rg-warsaw.png" />
    <h1>Welcome to my awsome diary</h1>
    <p>How are you?</p>
  </body>
</html>